Be in `harmony/harmony_website` directory.
Run `python3 -m manage makemigrations` to get all migrations.
Run `python3 -m manage migrate` to apply the migrations.

Run the server using `python3 -m manage runserver` for development purposes.
